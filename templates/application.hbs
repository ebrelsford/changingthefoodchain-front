<header>
    <div id="logo">{{#link-to 'index'}}{{t "title"}}{{/link-to}}</div>
    <div id="search">
        {{input type="search" action="search" value=searchText placeholder="Search by name, city, state"}}
    </div>
    <nav id="nav">
        {{#link-to 'about'}}about{{/link-to}}
        {{#link-to 'contact'}}contact{{/link-to}}
    </nav>
    <div id="languages">
        <a href="#" {{action 'setLocale' 'en'}}>english</a>
        <a href="#" {{action 'setLocale' 'es'}}>español</a>
    </div>
</header>

<div id="map"></div>

<div id="filters">
    <section class="filters-type">
        <h3>organization type</h3>
        {{view App.OrganizationTypeView content=organizationTypes}}
    </section>
    <section class="filters-sector">
        <h3>industry type</h3>
        {{view App.SectorView content=sectors}}
    </section>
</div>

<div id="popup">
    {{outlet popup}}
</div>

<div id="page">
    {{outlet page}}
</div>

{{outlet modal}}
